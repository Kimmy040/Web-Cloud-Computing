<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Movie Search</title>
    <style>
      .container {
        max-width: 800px;
        margin: 0 auto;
        text-align: center;
      }
      h1 {
        font-size: 2em;
        margin-top: 40px;
      }
      form {
        margin-top: 40px;
        display: inline-block;
        text-align: left;
      }
      input[type="text"],
      select {
        padding: 10px 20px;
        font-size: 1.2em;
        margin-bottom: 20px;
        width: 100%;
      }
      input[type="submit"] {
        background-color: #333;
        color: #fff;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 1.2em;
      }
      input[type="submit"]:hover {
        background-color: #555;
      }
      .results {
        margin-top: 40px;
      }
      .movie {
        display: inline-block;
        width: 200px;
        text-align: center;
        margin: 0 20px 20px 0;
        border: 1px solid #ddd;
        padding: 20px;
        vertical-align: top;
      }
      .movie h2 {
        font-size: 1.2em;
        margin-bottom: 10px;
      }
      .movie p {
        margin-bottom: 10px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Movie Search</h1>
      <form>
        <input type="text" id="keyword" placeholder="Enter keyword">
        <br>
        <select id="genre">
          <option value="">Select genre</option>
          <option value="action">Action</option>
          <option value="comedy">Comedy</option>
          <option value="drama">Drama</option>
          <option value="horror">Horror</option>
          <option value="romance">Romance</option>
        </select>
        <br>
        <input type="submit" value="Search">
      </form>
      <div class="results">
        <!-- Results will be displayed here -->
      </div>
    </div>
    <script>
      // Fetch movies from Watchmode API and display results
      const form = document.querySelector("form");
      form.addEventListener("submit", e => {
        e.preventDefault();
        const keyword = document.querySelector("#keyword").value;
        const genre = document.querySelector("#genre").value;
        let url = "https://watchmode.com/api/movies?";
        if (keyword) {
          url += `
